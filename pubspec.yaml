name: fast_operation
version: 1.6.0
description: A dart package for essential quick conversions for physical properties like distance, weight, temperature. Just import one file,then access all of its properties.
repository: https://github.com/shantanu1k/fast_operation/
environment:
  sdk: ">=2.7.0 <3.0.0"
dev_dependencies:
  flutter_test:
    sdk: flutter